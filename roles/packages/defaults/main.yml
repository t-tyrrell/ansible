---
# ============================================
# PACKAGES ROLE - DEFAULT VARIABLES
# ============================================

# Update all packages to latest
packages_update_all: true

# Base packages to install
packages_base_packages:
  - vim
